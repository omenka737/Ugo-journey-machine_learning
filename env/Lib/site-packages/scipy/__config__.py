# This file is generated by SciPy's build process
# It contains system_info results at the time of building this package.
from enum import Enum

__all__ = ["show"]
_built_with_meson = True


class DisplayModes(Enum):
    stdout = "stdout"
    dicts = "dicts"


def _cleanup(d):
    """
    Removes empty values in a `dict` recursively
    This ensures we remove values that Meson could not provide to CONFIG
    """
    if isinstance(d, dict):
        return { k: _cleanup(v) for k, v in d.items() if v != '' and _cleanup(v) != '' }
    else:
        return d


CONFIG = _cleanup(
    {
        "Compilers": {
            "c": {
                "name": "clang-cl",
                "linker": r"lld-link",
                "version": "20.1.8",
                "commands": r"clang-cl.exe",
                "args": r"-IC:/Users/task_178236955794638/croot/scipy_1782369647777/work/scipy/linalg, -IC:/Users/task_178236955794638/croot/scipy_1782369647777/work/scipy/_lib",
                "linker args": r"",
            },
            "cython": {
                "name": r"cython",
                "linker": r"cython",
                "version": r"3.2.5",
                "commands": r"cython",
                "args": r"",
                "linker args": r"",
            },
            "c++": {
                "name": "clang-cl",
                "linker": r"lld-link",
                "version": "20.1.8",
                "commands": r"clang-cl.exe",
                "args": r"",
                "linker args": r"",
            },
            "fortran": {
                "name": "llvm-flang",
                "linker": r"lld-link",
                "version": "20.1.8",
                "commands": r"flang.exe",
                "args": r"-D_CRT_SECURE_NO_WARNINGS, -fms-runtime-lib=dll, -fuse-ld=lld, -IC:/Users/DELL/Desktop/Sample_Project_1/env/Library/include",
                "linker args": r"-D_CRT_SECURE_NO_WARNINGS, -fms-runtime-lib=dll, -fuse-ld=lld, -IC:/Users/DELL/Desktop/Sample_Project_1/env/Library/include",
            },
            "pythran": {
                "version": r"0.18.1",
                "include directory": r"../../_h_env/Lib/site-packages/pythran"
            },
        },
        "Machine Information": {
            "host": {
                "cpu": r"x86_64",
                "family": r"x86_64",
                "endian": r"little",
                "system": r"windows",
            },
            "build": {
                "cpu": r"x86_64",
                "family": r"x86_64",
                "endian": r"little",
                "system": r"windows",
            },
            "cross-compiled": bool("false".lower().replace('false', '')),
        },
        "Build Dependencies": {
            "blas": {
                "name": "mkl-sdl",
                "found": bool("true".lower().replace('false', '')),
                "version": "2025",
                "detection method": "pkgconfig",
                "include directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/include",
                "lib directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/lib",
                "openblas configuration": r"unknown",
                "pc file directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/lib/pkgconfig",
                "has ilp64": bool(r"false".lower().replace('false', '')),
                "cython blas ilp64": bool(r"false".lower().replace('false', '')),
            },
            "lapack": {
                "name": "mkl-sdl",
                "found": bool("true".lower().replace('false', '')),
                "version": "2025",
                "detection method": "pkgconfig",
                "include directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/include",
                "lib directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/lib",
                "openblas configuration": r"unknown",
                "pc file directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/lib/pkgconfig",
                "has ilp64": bool(r"false".lower().replace('false', '')),
            },
            "pybind11": {
                "name": "pybind11",
                "version": "3.0.4",
                "detection method": "pkgconfig",
                "include directory": r"C:/Users/DELL/Desktop/Sample_Project_1/env/Library/include",
            },
        },
        "Python Information": {
            "path": r"C:/Users/DELL/Desktop/Sample_Project_1/env/python.exe",
            "version": "3.14",
        },
    }
)


def _check_pyyaml():
    import yaml

    return yaml


def show(mode=DisplayModes.stdout.value):
    """
    Show libraries and system information on which SciPy was built
    and is being used

    Parameters
    ----------
    mode : {`'stdout'`, `'dicts'`}, optional.
        Indicates how to display the config information.
        `'stdout'` prints to console, `'dicts'` returns a dictionary
        of the configuration.

    Returns
    -------
    out : {`dict`, `None`}
        If mode is `'dicts'`, a dict is returned, else None

    Examples
    --------
    >>> import scipy
    >>> scipy.show_config()
    ... # formatted output is printed to the console

    >>> config_dict = scipy.show_config(mode='dicts')
    >>> list(config_dict.keys())
    ['Compilers', 'Machine Information', 'Build Dependencies', 'Python Information']

    Notes
    -----
    1. The `'stdout'` mode will give more readable
       output if ``pyyaml`` is installed

    """
    if mode == DisplayModes.stdout.value:
        try:  # Non-standard library, check import
            yaml = _check_pyyaml()

            print(yaml.dump(CONFIG))
        except ModuleNotFoundError:
            import warnings
            import json

            warnings.warn("Install `pyyaml` for better output", stacklevel=1)
            print(json.dumps(CONFIG, indent=2))
    elif mode == DisplayModes.dicts.value:
        return CONFIG
    else:
        raise AttributeError(
            f"Invalid `mode`, use one of: {', '.join([e.value for e in DisplayModes])}"
        )
